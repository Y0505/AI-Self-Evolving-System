{
  "name": "ai-self-evolving-system",
  "version": "0.1.0",
  "private": true,
  "description": "An open-source experiment in autonomous software development.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --import tsx --test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
